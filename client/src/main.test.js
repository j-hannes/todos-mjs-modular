require('../setup/test')

require('./main')

describe('Main :: Program', function() {
  'use strict'

  it('waiting for implementation', function() {
    // there is nothing to test for now, but we still need to cover main.js
  })
})
